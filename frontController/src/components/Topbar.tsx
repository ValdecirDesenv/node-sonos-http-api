import React from "react";
import { Link } from "react-router-dom";

const Topbar = () => {
  return (
    <nav className="scaled-background navbar navbar-expand-lg navbar-light bg-light fixed-top">
      <p>Topbar</p>
    </nav>
  );
};

export default Topbar;
